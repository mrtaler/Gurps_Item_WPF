<Window x:Class="Item_WPF.EditContentInSimpleTable"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Item_WPF"
        mc:Ignorable="d"
        Title="Edit_content_in_simple_Table" Height="300" Width="533.803">
    <Grid>
        <DataGrid x:Name="TypeOfDamageDataGrid" Margin="10,10,10,53" ItemsSource="{Binding}" AutoGenerateColumns="False" RowEditEnding="TypeOfDamage_dataGrid_RowEditEnding" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="id"         Binding="{Binding id, Mode=TwoWay}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Name"       Binding="{Binding name, Mode=TwoWay}"/>
                <DataGridTextColumn Header="Long Name"   Binding="{Binding LongName, Mode=TwoWay}"/>
                <DataGridTextColumn Header="mDamage"   Binding="{Binding mDamage, Mode=TwoWay}"/>
                         </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="CloseButton" Content="Close" HorizontalAlignment="Left" Margin="10,0,0,10" Width="75" Height="20" VerticalAlignment="Bottom" Click="Close_button_Click"/>

    </Grid>
</Window>
