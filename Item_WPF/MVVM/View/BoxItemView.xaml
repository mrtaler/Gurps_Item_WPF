<Window
    x:Class="Item_WPF.MVVM.View.BoxItemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Item_WPF.MVVM.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="BoxItemView"
    Width="659.5"
    Height="300"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150*" />
            <ColumnDefinition Width="150*" />
        </Grid.ColumnDefinitions>
        <ListBox
            x:Name="listBox"
            Margin="10,37,25,10"
            DisplayMemberPath="szItemName"
            ItemsSource="{Binding ItemsFromDB}"
            SelectedItem="{Binding itemtobox}" />
        <Button
            x:Name="button"
            Height="25"
            Margin="252,7,25,0"
            VerticalAlignment="Top"
            Command="{Binding AddCommand}"
            Content="Add"
            RenderTransformOrigin="0.521,0.432" />
        <Button
            x:Name="button1"
            Grid.Column="1"
            Height="25"
            Margin="10,7,0,0"
            VerticalAlignment="Top"
            Content="Rem" HorizontalAlignment="Left" Width="68" />
        <ComboBox
            x:Name="comboBox"
            Width="111"
            Margin="10,10,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            DisplayMemberPath="name"
            ItemsSource="{Binding ItemsClass}"
            SelectedValue="{Binding SelectedItClassforSort}"
            SelectedValuePath="id" />
        <DataGrid
            x:Name="dataGrid"
            Grid.Column="1"
            Margin="10,37,10,10"
            ItemsSource="{Binding boxItem}" 
            AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Width="20"
                                    Binding="{Binding uiIndex,
                                                      Mode=OneWay}"
                                    Header="id"
                                    IsReadOnly="True" />
                <DataGridTextColumn  Binding="{Binding ITEM.szItemName,
                                                      Mode=OneWay}"
                                    Header="name"
                                    IsReadOnly="True" />

                <DataGridTextColumn  Binding="{Binding CountItems,
                                                      Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                    Header="Count"
                                    IsReadOnly="False" />
            </DataGrid.Columns>
        </DataGrid>
        <Label
            x:Name="label"
            Grid.Column="1"
            Width="144"
            Height="25"
            Margin="83,7,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="{Binding bx.nameOfBox}" />
        <Button x:Name="button2" Content="save" Command="{Binding Save}" HorizontalAlignment="Left" Height="25" Margin="172,7,0,0" VerticalAlignment="Top" Width="75"/>

    </Grid>
</Window>
